x=3.16
y=4
z=5

#to round the number or values
# result=round(x)

#to find values distance away from negative or zero
# result=abs(y)

#to find the powered result here its 4*4*4. 4 is the value and 3 is how many times needs to power.
# result=pow(4,3)

#to find the maximum value or number
# result= max(x,y,z)

#to find the minimum value or number
result= min(x,y,z)
print(result)